# EJEMPLO PRINCIPIOS SOLID:

Consideraciones:
- Java 21
- Maven

1. COMPILACION:
   - Desde linea de comando tener instalado maven y desde la raíz del proyecto ejecutar
    mvn package

2. EJECUCION
   - Desde linea de comandos ejecutar en la ruta donde se deposito el archivo jar generado de la compilacion target
   java -jar ejemplo-solid-1.0-SNAPSHOT.jar
